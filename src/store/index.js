import { createStore } from "vuex";
import { auth, self, user } from "./modules";

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    auth,
    self,
    user,
  },
});
